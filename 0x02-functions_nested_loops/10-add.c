#include "main.h"

/**
 * add - adds two integers and returns the result
 * @m: int to be added to b
 * @n: int to be added to a
 * Return: sum of a and b
 */

int add(int m, int n)
{
	int o;

	o = m + n;

	return (o);
}
